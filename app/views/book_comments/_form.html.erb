<div>
  <%# byebug %>
  <%= form_with model: [book, book_comment], url: book_book_comments_path(book_id: book.id), method: :post do |f| %>
    <%= f.text_area :comment, rows: '5', placeholder: "コメントをここに" %>
    <%= f.submit "送信する" %>
  <% end %>
</div>